/* Este codigo se trata de ordenar arreglos.
* sort() => Ordena alfabeticamente
* reverse() => Ordena el array de atras hacia adelante
*/
var aArregloAOrdenar = ["A", "C", "B", "Z"]
console.log(aArregloAOrdenar.reverse())
// [Z, B, C, A]
console.log(aArregloAOrdenar.sort())
// [A, B, C, Z]